{
  "name": "family-os-bb-data-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NAS-internal typed MariaDB API for the Family OS BB iPad application.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check lib.mjs && node --check migrate.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "mysql2": "3.14.4"
  },
  "engines": {
    "node": ">=22"
  }
}
